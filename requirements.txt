# Multi-Agent Framework & LLM Integration
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-groq>=0.2.0          # For LLaMA 3.3-70B via Groq API

# Email Verification Agent & RAG Pipeline
faiss-cpu>=1.8.0               # Vector store for SpamAssassin embeddings
textblob>=0.18.0               # Sentiment analysis on email body content
tldextract>=5.1.0              # Extraction of base domain from URLs[cite: 1]
dnspython>=2.6.0               # DNS resolution and domain validation[cite: 1]
nltk>=3.9.0                    # Email header & body tokenization[cite: 1]
beautifulsoup4>=4.12.0         # HTML stripping from email body[cite: 1]

# IP Range Analyzer & Vulnerability Inspection
python-nmap>=0.7.1             # Active scanning wrapper for Nmap[cite: 1]
requests>=2.32.0               # Queries to NVD CVE REST API[cite: 1]

# Log Processing & Data Analysis
pandas>=2.2.0                  # Normalization & feature extraction for CIC-IDS 2017[cite: 1]
elasticsearch>=8.15.0          # ELK Stack client integration[cite: 1]

# Environment & Schema Validation
python-dotenv>=1.0.1           # Environment variable management (.env API keys)
pydantic>=2.9.0                # Structured output schema parsing & LangGraph state validation